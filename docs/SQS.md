# python code
* `WaitTimeSeconds` : long polling in reciever
* `MessageGroupId` : Use the same id in sender and reciever
* `MessageDeduplicationId` : unique thing for deduplication
* `VisibilityTimeout` : not visible to other recievers time , not too small (reciept handle will expire)

