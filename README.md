# aws 
* sample

/test2/{proxy+}

default-mysql-client


curl -ivL -H 'Host: arp.com' https://192.168.49.2:30100

curl -X POST https://d1l77n2boazy2j.cloudfront.net \                       
   -H "Content-Type: application/json" \
   -d 'IamABadRequest'


%s/foo/bar/g


cat <<EOF > config.conf
hello
   boi
EOF
https://www.server-world.info/en/


```

{
  "name": "John",
  "age": 30,
  "city": "New York"
}
name=$(echo "$json" | grep -o '"name": *"[^"]*' | sed 's/"name": *"//')
age=$(echo "$json" | grep -o '"age": *[0-9]*' | sed 's/"age": *//')

```

* [scenario](./ecommerce_app) for redis, mysql